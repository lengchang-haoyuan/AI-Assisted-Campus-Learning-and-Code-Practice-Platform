# 开发节点使用顺序

## 当前执行入口

P00-P15 为原毕业设计阶段，已有工程与交付记录，不要重新初始化。P16-P24 为校园试点后续阶段，当前只有提示词，实际进度以 Git、代码和最新报告为准。

继续开发请从 [P16 试点需求与数据设计](../AI提示词/P16_试点需求与数据设计.md) 开始，完整文件清单和共同约束见 [AI 提示词使用说明](../AI提示词/README.md)。一次只投喂一个节点，不把路线图当作所有依赖、API、数据库或上线变更的授权。

- P16：只做需求、权限和数据设计，先确认，不开发业务。
- P17-P19：校园身份、班级、教学任务、提交和反馈。
- P20-P22：AI 页面、社区治理、质量和成本控制。
- P23：生产等价验证、安全、隐私和试点准入。
- P24：取得真实参与和批准后开展试点，未发生的观察不能伪造。

后续保持现有 Vue/FastAPI/MySQL 单体，不默认新增 Redis、消息队列、向量库、微服务或多供应商。原个人课程与教学班级分开授权，只有具体阶段确认后才执行增量迁移。检查通过后更新中文日志与报告，仅提交本节点改动、推送并核对远端。

## 历史 P00-P15 规划

下列“5 小时第一轮”和 Redis、多 Provider 等内容是原始规划背景，不是当前实际完成时间、已安装能力或后续必须新增的技术要求；实际代码与上面的后续约束优先。

按 P00 → P15 顺序执行。一次只给 AI 一个文件。

## 5小时第一轮
优先完成：
P00 工程初始化
P01 数据库
P02 后端架构
P03 JWT
P04 Project CRUD
P05 前端基础

每个进度点完成后：
测试与验收 → 更新操作日志和完成报告 → 自己理解 → Git 提交并推送到 `origin/main` → 核对远端提交哈希 → 再进入下一个。

项目技术基线：
Vue 3 + TypeScript + Vite + Element Plus + Vue Router + Pinia + Axios + ECharts + Vue Flow；
Python + FastAPI + SQLAlchemy + Pydantic + JWT；
MySQL + Redis；
AI Provider：OpenAI / DeepSeek / Claude。
