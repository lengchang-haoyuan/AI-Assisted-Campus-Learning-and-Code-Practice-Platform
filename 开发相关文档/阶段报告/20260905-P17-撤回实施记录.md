# P17 撤回实施记录

时间：2026-09-05 00:42 +08:00。

1. **文件清单**：恢复 backend/app 下 models/user.py、models/__init__.py、core/security.py、services/auth.py、main.py 的 P17 改动；仅撤回 api/deps.py 的 P17 部分；删除 models/campus.py、repositories/campus.py、services/campus.py。追加操作日志和本记录。
2. **设计与调用链**：用户要求撤回，停止 P17 实施；认证恢复原有调用链，保留此前工作流教学节点注册和其他已有修改。P16 设计文档保留。
3. **启动与环境**：本次及刚才 P17 实施均未重启服务、修改环境或执行数据库迁移，无需数据库回退。
4. **检查结果**：git diff 确认五份恢复文件与 HEAD 一致，deps.py 仅剩此前教学节点改动；rg 检查后端源码，无 TokenIdentity、auth_version 或 campus 模块引用残留。
5. **手工验收**：逐项核对撤回范围，未撤回此前工作流、项目库排版、P16 文档及其他工作区修改。
6. **未执行与边界**：未运行测试、Lint、类型检查、构建或浏览器验收；此次为精准撤回，不报告功能测试通过。未创建真实账号。
7. **Git**：按用户要求，不提交、不推送；此前 P16 已有提交不变。
