# P21 现状检查与实施待确认报告

状态：现状检查和可审查实施方案已完成；数据库、公共 API、历史内容处理和校园访问变化等待用户确认，业务尚未实施。

## 1. 修改和新增文件清单

- 新增 `开发相关文档/开发规划/P21_校园社区治理实施方案.md`。
- 新增本报告，并向 `开发相关文档/开发日志/开发操作日志.md` 追加本次预检记录。
- 保留工作区中既有项目说明和答辩指南修改；未修改运行时代码、依赖、锁文件、数据库、服务或历史迁移。

## 2. 本阶段关键设计和调用链

实际社区仍是拥有者直接切换 `Project.is_published`，社区读取实时 Project，所有接口只要求登录。推荐改为 作者申请不可变公开快照 → 管理员审核 → 有效校园成员浏览互动 → 举报/下架 → 作者复核，并复用 P17 账号处罚及 P19 本人通知。

具体四张增量表、两个现有表调整、状态、权限、历史内容和 API 契约见 [P21 实施方案](../开发规划/P21_校园社区治理实施方案.md)。方案不会授予管理员读取私人 Project、Context、学习笔记、AI 输入或教学提交的权限。

## 3. 启动命令及必要环境变量

本轮只读检查，不需要重启服务或新增环境变量。确认实施后沿用现有 FastAPI、Vite 和 MySQL 配置，不新增依赖、Redis、消息队列或外部平台。

## 4. 执行过的测试、Lint、类型检查、构建和结果

执行 Git 状态、Router/Service/Repository/Model/Schema、前端路由和页面、P16/P17/P19 文档及真实数据库聚合数量的只读核对。代码尚未修改，因此未把既往 203 项后端测试、前端类型检查或构建结果计为本轮通过。

## 5. 手工验收步骤和实际结果

确认现有直接发布、撤回、列表、详情、评论、点赞、收藏和浏览链路均没有治理状态或校园资格校验；确认账号停用审计可复用、通知表当前不能承载社区事件。真实 MySQL 当前有 2 个公开项目及少量互动，均未修改；治理闭环和浏览器角色流程尚未执行。

## 6. 未执行或失败的检查、已知问题和剩余风险

等待确认的变化包括四张新表、Comments/Notification 约束、直接发布 API 行为、有效校园成员访问门槛、历史内容过渡和频率上限。当前 1 个有效账号没有有效校园身份，实施推荐方案后将失去社区访问，但仍保留个人功能。

教师确认、校方批准、治理记录保留期限和版权判断均未取得。当前文档只能标记为技术设计待确认，不能写成社区治理已经上线或课堂效果已验证。

## 7. 建议的 Git commit 信息

完整实施并验收后：`feat: implement campus community moderation`。当前只形成待确认方案，不创建该功能提交，也不推送 GitHub。
