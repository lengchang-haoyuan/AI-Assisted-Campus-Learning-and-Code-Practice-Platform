# 任务完成报告：生成 ScholarHub 分阶段 AI 开发提示词

## 需求摘要

阅读项目的 `开发相关文档/开发规划/使用顺序.md`、`P00` 到 `P15` 规划，以及前后端目录职责文档，为从零开始开发 ScholarHub 生成可直接投喂给 AI 的提示词。

## 修改内容

- 新增 `开发相关文档/AI提示词/README.md`，说明使用方式、执行顺序、通用约束和固定汇报格式。
- 新增 `开发相关文档/AI提示词/P00_工程初始化.md` 至 `P15_全系统联调与答辩优化.md`。
- 将每个阶段整理为目标、范围边界、执行要求、验收标准和建议提交信息。
- 强化认证安全、资源所有权、API 分层、数据库约束、AI Provider 抽象、Agent 输出校验、Workflow 有界执行和最终联调要求。

## 文件与架构变化

仅新增 Markdown 文档和 AI 工作记录目录，没有修改业务代码、数据库、依赖或部署配置。当前项目目录暂无 Git 仓库，因此没有实际 Git 提交。

## 关键设计决定

- 严格保留 `P00 → P15` 顺序，每次只执行一个阶段。
- P09 只做 Workflow 编辑器，P10 建立 Context，P11 建立 Provider，P12 实现 Agent，P13 才执行完整 Workflow，避免 AI 模块依赖倒置。
- 所有阶段要求 AI 先扫描实际代码和配置，并在新增依赖、数据库变更、公共 API 变化或架构变化前说明风险。
- 将“AI 辅助学习与开发”作为边界，明确不做在线 IDE、任意代码执行、私信好友、复杂微服务和自研模型。

## 兼容性与安全影响

本次没有运行时兼容性影响。提示词要求不硬编码秘密、服务端执行授权、模型输出 schema 校验、限制模型调用预算和超时，并避免记录敏感 Prompt、Token 和个人信息。

## 验证结果

- 通过文件数量检查：提示词目录共 17 个文件，包括 README 和 P00-P15。
- 通过编号检查：P00-P15 文件全部存在且连续。
- 通过关键内容检查：技术栈、ProjectContext、AIProvider、WorkflowEngine、Statistics API 和验收要求均已写入相应文件。

## 未执行或未通过的检查

- 未执行前后端构建、测试或启动检查，因为本次只新增开发提示词，当前项目尚无业务代码。
- 未执行 Git 提交，因为当前目录不是 Git 仓库。

## 剩余风险与后续建议

开始执行 P00 前，先确认 Python、Node.js、MySQL、Redis 和包管理器的本地版本；P01/P11 涉及数据库和模型 Provider 配置时，先准备本地 `.env`，不要把真实密钥粘贴给 AI。每个阶段完成后再进行下一阶段，并保留阶段验证记录。
