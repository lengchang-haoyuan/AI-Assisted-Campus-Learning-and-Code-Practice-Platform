# P16 校园试点设计草案报告

时间：2026-09-05 00:30 +08:00。范围：只读现状核对与文档设计。

**设计草案已完成；用户设计确认、教师教学确认、校方试点批准均未取得。P17 未开始。**

## 1. 修改和新增文件清单

- 新增 [P16 校园试点最小方案与数据设计草案](../开发规划/P16_校园试点最小方案与数据设计草案.md)：唯一设计正文。
- 新增本报告。
- `开发日志/开发操作日志.md`：只追加本次 P16 记录；提交只包含本次新增段落，此前未提交的记录保留在工作区。

没有修改运行时代码、依赖或锁文件、Models、schema.sql、迁移、运行服务及真实账号。未改写旧阶段报告或提示词。

## 2. 关键设计和调用链

保留当前 Vue/FastAPI/MySQL 单体和 Provider/Agent/Workflow Engine。新增校园资格不改变旧 User 和个人 Course/Project 的所有权；教师只读本班已提交快照。教学链路为发布任务→版本提交→指定版本反馈/退回→重交→本人无时长学习事件→自愿校园发布，状态相互独立。

正文含权限矩阵、ER、字段/主外键/索引、状态/删除/归档语义、页面及 API 草案、邀请/改密/会话撤销、外发与留存、分阶段迁移及恢复方案。新增依赖不推荐；新 API、数据库、JWT 协议和部署建议全部待具体确认，不因本文件获得实施授权。

## 3. 启动命令及必要环境变量

P16 文档无需启动服务或新增环境变量。本次未读取秘密环境值；现有开发启动说明仍见根 README 与 backend/README。后续部署命令、费用上限和真实管理员初始化需在对应阶段确认。

## 4. 执行过的检查与结果

- 只读核对 Git 状态/提交/远端、项目规则、README、目录职责、P15 与最新实际报告、路由、认证、Models、schema.sql、迁移和前端入口。
- 文档检查：本地引用可解析，Markdown 围栏闭合，19 张现有 SQL 表计数与说明一致，关键确认/隔离/版本/删除/退出场景齐备；暂存差异仅本次三份文档，空白检查通过。
- 未运行代码测试、Lint、类型检查、构建、数据库验收、安全审计、模型调用或浏览器业务验收。正文引用的历史结果均标注为历史记录。

## 5. 手工验收步骤和实际结果

逐项对照权限矩阵、ER、字段表、接口和状态场景：学生互读拒绝、教师跨班拒绝、管理员私人数据默认拒绝；邀请与已有账号分离；重复提交有界幂等；退回生成新版本；退出/归档停止写入；取消项目发布不抹去教学快照。名称与关系已统一，发现的账号版本/资格版本及历史只读例外已澄清。

以上是文档一致性检查，未执行未来校园业务。负责人、课程、教师、学生范围、访问条件、付款者、维护者及政策均保持待确认；没有虚构人工验收或学校批准。

## 6. 未执行检查、问题与剩余风险

当前工作区有此前未提交功能，正文以工作区现状描述，不能认为它们已经随本次文档发布。P15 中部分“仅 API”叙述已过时，当前工作流运行/结果页面和教学节点已有实现；通用 Context/独立 Agent 等仍缺页面。真实数据库本次未连接。

P17 必须先取得正文第 11 节的 D1–D4 等具体确认；数据外发、费用、留存与生产部署仍阻塞真实试点。所列期限是建议，不是法律结论。提交设计不等于批准开发或试点。

## 7. Git commit 信息

`docs: define campus pilot scope and data design`

按现有约定仅提交本次草案、报告和日志追加段，再正常推送 origin/main 并核对提交哈希；不强推，不提交之前的功能改动。具体提交与远端核对结果见本次最终汇报。后续如否决草案，可用新的文档修订提交标记废止；没有数据库回退操作。
