# 工作流 AI 入口与教学节点验收

时间：2026-09-05（Asia/Shanghai）。范围：检查 DeepSeek 可用性，补齐工作流执行与结果展示，新增解题提示、代码讲解、答案评审。

## 交付

- 前端：运行/保存并运行按钮、运行中锁定、未应用配置提醒、上下文初始化与同步、未完成续跑/全部重跑、轮询状态、分页历史、中文结构化结果和错误提示。
- 后端：三个教学 Agent 的输入和输出校验、生产注册表、教学结果持久化；空代码或空题意在收费请求前拒绝；教学结果不改写 ProjectContext。
- 修复实测发现的两个必要问题：只读教学结果无需构造非空上下文补丁；代码或节点配置改变后将旧结果标记待更新，以便续跑。
- 未新增依赖或迁移。没有提交 Git 或发布部署。

## 真实验证

后端按 `.env` 重启。用户的 DeepSeek Key 有效；此前启动环境存在同名旧变量覆盖文件配置。本次启动会话清除该变量，未修改系统环境和密钥。

模型：`deepseek-v4-flash`。最小 JSON 实际生成成功，40 tokens。随后创建关联 Python 001 颠倒字符串项目（ID 89）的教学示例工作流 ID 22：

| 运行 | 验证方式 | 结果 | 用量 |
| --- | --- | --- | --- |
| 28 | 生产 Service、MySQL、真实 Provider | 三个教学节点全部完成 | 3667 tokens |
| 29 | 浏览器点击“运行 AI”，全部重跑 | 三个教学节点全部完成，刷新后可查看 | 3666 tokens |
| 30 | 浏览器修改评审代码为直接返回原字符串，应用配置，保存并运行 | 仅评审节点重跑，结论 needs_revision，正确指出未反转 | 1207 tokens |

示例入口：<http://127.0.0.1:5173/workflows/22>。运行 29 可查看全部教学结果，运行 30 展示错误答案的评审。评审节点当前保留错误示例代码，便于对照结果，用户可替换为自己的代码。项目学习状态仍为 not_started、进度 0。

## 检查

- `python -m unittest discover -s tests -q`：160 项通过，包括新增教学结果回读、只读上下文、输入预检、失败续跑和配置变更失效测试。
- `npm run build`：类型检查和生产构建通过；既有 Analytics 分包大于 500 kB 警告保留。
- `python -m pip check`：无依赖冲突。
- `git diff --check`：通过。项目未配置独立 Lint/Formatter 命令。
- 浏览器：运行中按钮禁用、应用配置前运行被阻止、保存并运行、部分续跑、运行历史刷新、中文结果均验证通过；控制台无错误。
- 桌面 1440 × 1000、手机 390 × 844 检查结果面板可读，已恢复浏览器默认尺寸。
- Impeccable detector 对此次 UI 文件无发现。仓库残留 P05 首页 comp 的未完成 gate 与本次工作流功能无关，未扩展至首页改版。

## 使用边界

答案评审属于 AI 静态阅读，不执行 Python，也不代表测试通过。旧三节点仍面向项目分析，质量检查节点尚不支持 AI 执行。当前同步执行接口最长等待依后端超时配置；离开页面可回来查看历史，网络请求中断后应先刷新确认状态再重跑。
