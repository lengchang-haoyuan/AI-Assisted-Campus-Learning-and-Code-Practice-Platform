# 开发相关文档整理报告

## 1. 修改和新增文件清单

- 将根目录 P00-P15 阶段规划移动到 `开发相关文档/开发规划/`。
- 将原开发记录目录下的操作日志、阶段报告和 P15 浏览器截图分别移动到 `开发日志/`、`阶段报告/` 和 `测试证据/P15/`。
- 将 P15 演示指南移动到 `开发相关文档/答辩材料/`。
- 将根目录的界面设计说明和界面验收记录移动到 `界面设计/`，并统一改为中文文件名。
- 将 `开发相关文档/AI提示词/` 中 P09-P13 的英文阶段文件名改为中文名称。
- 新增 `开发相关文档/README.md`，说明分类、阅读顺序和入口文件约定。
- 同步更新根 README、后端说明、前端浏览器回归脚本及历史报告中的路径引用。

## 2. 分类和关键取舍

- `开发规划/`：阶段规划和执行顺序。
- `AI提示词/`：可直接投喂给 AI 的阶段提示词。
- `开发日志/`：按时间记录的操作和恢复信息。
- `阶段报告/`：P00-P15 的开发、联调、交付和安全审计报告。
- `测试证据/`：浏览器回归与响应式验收截图。
- `答辩材料/`：启动、演示和答辩说明。
- `界面设计/`：产品视觉规范和界面验收记录。

根目录 `README.md` 保留为 GitHub 项目入口，根目录 `PRODUCT.md` 保留为 Impeccable 产品设计清单；`backend/README.md` 保留为后端组件的启动和接口说明。这些入口文件不属于阶段报告，且保留原位置不会影响运行或既有工具约定。

## 3. 运行影响和兼容性

- 未修改前后端业务代码、依赖清单、锁文件、数据库结构、迁移、环境变量或 API。
- 前端回归脚本的证据目录已同步到 `开发相关文档/测试证据/P15/`。
- 历史文档中的旧资料路径和英文报告路径已同步替换。

## 4. 验证结果

- 旧路径和旧英文报告文件名扫描：通过，未发现残留引用。
- Markdown 相对链接存在性检查：通过，全部目标文件或目录存在。
- `git diff --check` 和暂存区差异检查：通过；Git 仅提示工作区换行符转换警告，没有空白错误。
- `git ls-files docs`：无结果，原 `docs` 目录下已无受 Git 跟踪的文件。
- 运行时测试、Lint、类型检查和构建：本次未重复执行。原因是本次仅整理文档、截图和验证脚本路径，没有改变生产代码；已有 P15 运行验证记录保存在本目录的阶段报告中。

## 5. 手工检查路径

1. 打开 `开发相关文档/README.md`，确认分类和阅读顺序。
2. 从 `开发相关文档/开发规划/使用顺序.md` 进入 P00-P15 规划。
3. 从 `开发相关文档/阶段报告/` 查看阶段交付与安全审计记录。
4. 按 `开发相关文档/答辩材料/P15演示与答辩指南.md` 执行启动和答辩演示。

## 6. 未完成项和剩余风险

- 未发现因本次移动产生的悬空 Markdown 相对链接。
- 根目录 `PRODUCT.md`、根目录 `README.md` 和 `backend/README.md` 仍使用约定文件名；改名会降低 GitHub、Impeccable 或组件启动说明的可发现性，因此没有强行中文化。
- 空的旧 `docs` 文件夹仅是本地目录残留，不属于 Git 文件，不参与项目运行。

## 7. 建议提交

`docs: organize development reports and records`
