# AI 操作日志

## 2026-08-29 21:51:53 +08:00

- 操作：阅读 `开发相关文档/开发规划/使用顺序.md`、`P00` 到 `P15` 规划文档，以及前后端目录职责文档
- 目标：理解从零开始建设 ScholarHub 所需的阶段顺序、技术栈、模块边界和验收要求
- 原因：为每个阶段生成可直接投喂给代码 AI 的提示词
- 结果：完成规划梳理；确认当前 ScholarHub 目录暂无业务代码和 Git 历史
- 恢复方式：本次仅新增文档，不涉及代码、数据库或外部系统状态

## 2026-08-29 21:52:00 +08:00

- 操作：新增 `开发相关文档/AI提示词/README.md` 及 `P00` 到 `P15` 提示词文件
- 目标：把规划转换为可逐阶段执行的独立开发任务
- 原因：用户要求从零开始按规划向 AI 投喂提示词
- 结果：完成；共 17 个提示词使用说明和阶段文件，编号连续
- 恢复方式：删除本次新增的提示词目录和文件即可；未修改原规划文档

## 2026-08-29 21:52:30 +08:00

- 操作：执行文件数量、编号连续性、关键术语和标题检查
- 目标：确认提示词文件完整且覆盖项目关键链路
- 原因：文档交付前校验
- 结果：通过；P00-P15 全部存在，关键技术和验收内容已覆盖
- 恢复方式：本次为只读检查，无需恢复

## 2026-08-29 22:16:33 +08:00

- 操作：完成 P00 前后端工程初始化、依赖安装和真实联调
- 目标：`frontend/`、`backend/`、根目录 README 与忽略规则
- 原因：建立 Vue 3 + TypeScript + Vite 与 FastAPI + Pydantic + SQLAlchemy 的最小可运行骨架
- 结果：完成；health、Swagger、CORS、前端加载/成功/失败/恢复状态、移动端布局、类型检查和生产构建均验证通过
- 恢复方式：本阶段只新增工程骨架；删除本次新增的 `frontend/`、`backend/`、根目录 `README.md` 和 `.gitignore` 对应内容即可恢复，未修改数据库或外部系统
- 验证：`pnpm typecheck`、`pnpm build`、Python `compileall`、`pip check`、HTTP health/docs/404/CORS、浏览器桌面与 390×844 视口检查
- 备注：TypeScript 最新 7.0.2 与当前 vue-tsc 不兼容，经确认固定为 5.9.3；当前目录不是 Git 仓库，未执行提交

## 2026-08-29 22:20:45 +08:00

- 操作：依据 `frontend-backend-code-standards` Skill 审查 P00 工程
- 目标：前端、后端、配置、依赖、验证记录和完成报告
- 原因：确认 P00 是否按用户编写的代码规范执行
- 结果：整体符合但不是完全符合；发现 `.env` 加载说明不一致、后端依赖未锁定、前端错误提示固定端口三个问题
- 恢复方式：本次仅新增审查记录和报告，未修改业务代码、依赖或运行配置
- 验证：重新执行 `pnpm typecheck`、`pnpm build`、Python `compileall`、`pip check` 和 health 请求，均通过

## 2026-08-29 22:45:26 +08:00

- 操作：完成 P01 MySQL 数据库设计、非破坏性建库脚本和 SQLAlchemy Models
- 目标：建立用户、项目、社区、课程学习、Workflow 和 AI 记录共 17 个核心实体及项目标签关联表
- 原因：为后续认证、CRUD、Workflow 和 AI 阶段建立统一持久化契约，本阶段不实现业务 API
- 结果：完成；`schema.sql` 已在本机空 MySQL 8.0 实例执行，建立 18 张表，实际数据库的 33 个外键、约束和索引与 Models 一致
- 恢复方式：源码可删除本次新增数据库文件并还原配置变更；数据库当前仅含空表，如需移除必须先人工确认后执行，不在本次任务中自动删除
- 验证：8 项 `unittest` 通过，Python `compileall` 和 `pip check` 通过，环境变量 SQLAlchemy 连接通过，实际库只读结构对照通过，P00 health 与 Swagger 回归通过
- 安全：数据库凭据仅通过交互式输入用于本次验证，未写入源码、`.env`、命令行参数、日志或报告；AI 请求表不保存 API Key、原始提示词或原始敏感输入
- 备注：新增依赖 `PyMySQL` 与 `pydantic-settings` 已在执行前说明事实、风险和推荐方案并经用户批准；当前目录不是 Git 仓库，未执行提交

## 2026-08-29 23:07:44 +08:00

- 操作：初始化 Git 仓库、建立 `origin`、提交 P00-P01 基线并推送 GitHub
- 目标：`https://github.com/lengchang-haoyuan/AI-Assisted-Campus-Learning-and-Code-Practice-Platform.git`
- 原因：用户要求发布当前项目，并要求以后每个节点验收后提交和推送
- 结果：完成；创建 `main`，基线提交为 `7cf42228e90cd403bc9d5d998034e1274683bd69`，本地与远端 `refs/heads/main` 哈希一致
- 恢复方式：本次不删除远端仓库或重写历史；如需撤销代码，应通过新的 revert 提交处理
- 验证：后端 8 项结构测试、Python 编译、`pip check`、前端类型检查和生产构建均通过；远端哈希通过 `git ls-remote` 核对
- 安全：暂存预览确认 `.env`、`.venv`、`node_modules`、构建产物和本地依赖缓存未进入提交，未提交真实数据库凭据
- 备注：根 README 和使用顺序已明确“每节点验证、更新报告、提交、推送并核对远端哈希”的交付流程

## 2026-08-30 09:10:45 +08:00

- 操作：完成 P02 FastAPI 后端分层基础架构
- 目标：建立 Router、Schema、Service、Repository、Model 的职责边界，并提供统一异常、结构化日志、请求 ID、CORS 和数据库 Session 依赖
- 原因：为后续认证和业务接口提供稳定基础，不在本阶段实现任何业务功能
- 结果：完成；health 响应保持兼容，未知路由及应用异常使用统一错误结构，Session 可通过 FastAPI 依赖获取并可靠关闭
- 恢复方式：回退本阶段提交即可；本次未修改数据库结构、数据或外部服务配置
- 验证：15 项 `unittest`、Python `compileall`、`pip check`、真实 HTTP health/Swagger/404/CORS、真实 MySQL Session 依赖 `SELECT 1` 均通过
- 安全：500 响应和结构化日志不记录异常原文、SQL、请求体、查询字符串或凭据；数据库密码仅交互用于验证，未持久化
- 备注：未新增第三方依赖；未实现注册登录、JWT、Project CRUD、社区、学习、Workflow 执行或 AI Provider

## 2026-08-30 09:25:39 +08:00

- 操作：完成 P03 用户注册、登录、JWT 和当前用户认证闭环
- 目标：实现 `POST /api/v1/auth/register`、`POST /api/v1/auth/login`、`GET /api/v1/users/me`，以及前端 Bearer Token 注入和 401 清理
- 原因：在 P02 分层基础上建立唯一的认证范围，不实现其他业务
- 结果：完成；真实 MySQL 验收得到注册 201、重复注册 409、错误密码 401、登录 bearer Token、当前用户 200、无效 Token 401
- 恢复方式：回退本阶段提交并移除新增依赖；本次未修改数据库结构，验收用户已删除
- 验证：24 项后端测试、Python `compileall`、`pip check`、前端类型检查和生产构建通过；真实数据库确认密码为 Argon2id 哈希且不等于明文
- 安全：JWT Secret 和数据库凭据仅通过环境变量或交互式输入使用，未写入代码、日志或报告；日志不记录密码、Token 和完整认证头
- 依赖确认：用户明确批准新增 `pwdlib[argon2]` 和 `PyJWT`；实际安装 `pwdlib 0.3.1`、`argon2-cffi 25.1.0`、`PyJWT 2.13.0`，未安装不需要的 `python-multipart`
- 备注：前端 Token 保存在 `sessionStorage`，收到 401 时同时清除存储和 Pinia 当前用户状态；未实现登录页面或业务路由

## 2026-08-30 09:39:12 +08:00

- 操作：完成 P04 Project REST CRUD、所有权校验、有界分页和稳定排序
- 目标：实现 `GET/POST /api/v1/projects` 与 `GET/PUT/DELETE /api/v1/projects/{id}`，所有接口仅允许当前登录用户访问其项目
- 原因：在 P03 认证闭环上建立第一个数据库持久化业务模块，不提前实现社区、Workflow、AI 或前端页面
- 结果：完成；真实 HTTP + MySQL 验证创建 201、跨用户访问/更新/删除 403、更新持久化、删除 204、删除后 404 和空列表语义
- 恢复方式：回退本阶段提交；本次没有修改数据库结构，验收项目和 2 名临时用户均已删除
- 验证：38 项后端测试和 OpenAPI 契约检查通过；Python 编译、依赖检查、前端回归检查和生产构建见单次完成报告
- 安全：owner 只来自 JWT 当前用户，客户端 owner 伪造返回 422；Service 对详情、更新和删除统一执行所有权检查
- 依赖与数据：未新增依赖、未修改 `schema.sql` 或 SQLAlchemy Models；本机 `mysql` CLI 不在 `PATH`，验收清理改用项目已有 SQLAlchemy/PyMySQL 并确认无测试用户残留

## 2026-09-01 12:26:09 +08:00

- 操作：完成 P05 Vue 前端基础框架、认证页面、登录后布局、Project CRUD 页面、个人工作台和个人资料页
- 目标：实现 `/login`、`/register`、`/`、`/projects`、`/projects/:id`、`/workspace/dashboard`、`/profile`，并通过真实 FastAPI API 完成首条业务闭环
- 原因：在 P03/P04 认证和 Project API 基础上建立可运行的前端业务入口，不提前实现 P06 社区、P07 学习数据或 P09 Workflow
- 结果：完成；Router、Pinia、Axios Token 注入和 401 清理、认证状态、项目列表/创建/详情/编辑/删除确认、Loading/空/失败/重试状态均已实现
- 恢复方式：回退本阶段提交；本次未修改公共 API、数据库结构或依赖，临时验收项目和账号已清理
- 验证：前端类型检查与生产构建通过；后端 38 项测试、Python 编译、`pip check`、MySQL 连接和结构检查通过；浏览器真实验证全部 P05 路由、CRUD、390×844 响应式、液态筛选、数字滚动、卡片翻面和失败重试，控制台无错误
- 设计：采用“轻盈实验室布告板”基调并记录 `开发相关文档/界面设计/界面设计说明.md`；最终设计检测器无问题。严格 HERO 像素门为 55% 且保持 open，因为参考图包含 P06/P07/P09 后续数据，当前阶段未用虚构数据换取像素一致
- 安全：Token 保存在 `sessionStorage`，前端路由守卫仅改善导航体验，所有资源权限仍由后端 JWT 与 owner 校验；未提交数据库凭据、JWT Secret、测试密码或 Token

## 2026-09-01 17:27:17 +08:00

- 操作：完成 P06 校园代码社区、项目发布、标签筛选、评论、点赞、收藏和浏览量闭环
- 目标：在现有 Project 与社区表结构上实现真实 API、前端社区列表与详情、个人项目发布入口及互动状态
- 原因：按 P06 范围连接校园代码社区与个人项目库，不进入私信、关注、实时聊天、学习系统或 AI 能力
- 结果：完成；新增 `/api/v1/community/*` 浏览接口及项目评论、点赞、收藏、浏览、发布/撤下接口，前端新增 `/community` 和 `/community/projects/:id`
- 恢复方式：回退本阶段提交；本次未新增依赖、未修改 `schema.sql` 或 SQLAlchemy Models，临时验收用户、项目和孤立标签已清理
- 验证：后端 47 项测试、Python 编译、`pip check`、MySQL 连接通过；前端类型检查、生产构建和生产依赖审计通过；真实 MySQL 两用户联调覆盖 403/404/204、重复互动幂等和刷新持久化
- 设计：桌面与 390×844 窄屏、液态标签筛选、数字滚动、卡片翻面、社区详情与评论状态通过浏览器检查，控制台无 warning/error，Impeccable detector 返回空问题列表
- 安全：所有社区接口要求 JWT；发布/撤下校验项目 owner，评论删除校验评论作者；响应不返回 ORM、认证头、Token、数据库凭据或内部异常
- 已知限制：P05 参考图 HERO 门仍为 55% open，属于此前包含 P06/P07/P09 未来内容的像素复刻任务；P06 新页面按现有 `开发相关文档/界面设计/界面设计说明.md` 扩展，未为像素分数伪造后续业务数据

## 2026-09-01 17:51:50 +08:00

- 操作：完成 P07 个人工作台、任务管理、项目进度和学习记录闭环
- 目标：实现 `/workspace/dashboard`、`/workspace/tasks`、`/workspace/projects`、`/workspace/projects/:id`、`/workspace/records` 及对应真实 API
- 原因：在 P03/P04/P06 基础上为当前用户聚合任务、项目与学习记录，不进入 P08 学习计划、AI、Workflow 或报告能力
- 结果：完成；Dashboard 由数据库聚合今日任务、学习分钟、任务完成率和项目统计，任务支持创建/完成/关联项目，学习记录支持内容、类型、时长和项目关联
- 恢复方式：回退本阶段提交；本次未新增依赖、未修改 `schema.sql`、SQLAlchemy Models 或迁移，浏览器验收数据已按外键依赖顺序清理
- 验证：后端 54 项测试、Python 编译、`pip check`、MySQL 连接通过；前端类型检查、1712 modules 生产构建和生产依赖审计通过；真实 MySQL 与浏览器完成任务和记录闭环
- 设计：沿用轻盈实验室布告板基调，使用液态任务筛选、数字滚动和动画进度；桌面与 390px 窄屏无横向溢出，控制台无 warning/error
- 安全：任务、记录和工作台项目全部按 JWT 当前用户隔离，Service 校验关联项目和任务所有权；未提交数据库凭据、测试密码、Token 或内部异常
- 已知限制：Impeccable 单次检测发现的 3 个统计卡顶部强调线已移除；按检测协议未重复运行。P05 HERO 门仍为 55% open，与 P07 功能验收无关

## 2026-09-01 22:35:17 +08:00

- 操作：完成 P08 课程、学习计划、每日任务和学习记录完整业务闭环
- 目标：新增 `/api/v1/courses`、`/api/v1/learning/*` 分层 API，以及 `/learning`、`/learning/tasks`、`/learning/records` 前端页面
- 原因：在 P07 个人工作台基础上建立确定性的学习系统，为后续统计和报告保留真实结构化数据，不接入 AI
- 结果：完成；课程、计划、任务和记录均支持完整读写，任务完成与计划进度在同一事务中保持一致，P07 旧完成入口同步兼容计划进度
- 恢复方式：回退本阶段提交；本次未新增依赖、未修改 `schema.sql`、SQLAlchemy Models 或迁移，临时验收数据已清理
- 验证：后端 62 项测试、Python 编译、`pip check`、MySQL 连接通过；前端类型检查、1730 modules 生产构建和生产依赖审计通过；真实 HTTP + MySQL 验收计划进度达到 100%
- 设计：沿用轻盈实验室布告板基调，新增液态学习导航、数字滚动和 transform 进度动画；桌面与 390px 窄屏无横向溢出，控制台无错误
- 安全：全部资源按 JWT 当前用户隔离，Service 校验 plan/project/course/task 所有权；未提交数据库凭据、JWT Secret、测试密码、Token 或内部异常
- 已知限制：仓库仍无前端 Lint 和组件测试脚本；P05 HERO 门属于既有参考图验收项，与 P08 功能无关

## 2026-09-01 23:24:42 +08:00

- 操作：完成 P09 Workflow 持久化分层 API 与 Vue Flow 可视化编辑器
- 目标：实现 Workflow、Node、Edge CRUD、整图原子保存、图合法性校验，以及 `/workflows` 和 `/workflows/:id` 前端闭环
- 原因：在认证和 Project 所有权基础上建立可保存、可恢复的可视化开发流程，本阶段不接 AI、不执行 Agent
- 结果：完成；两个节点可添加、配置、拖拽和连线，保存后版本递增，刷新恢复位置、配置和边，过期版本与循环图均返回 409 且不污染原图
- 恢复方式：回退本阶段提交并移除 `@vue-flow/core`；本次未修改数据库结构、Models 或迁移，临时验收账号、项目和工作流已清理
- 验证：后端 72 项全量测试和 10 项 Workflow 专项测试通过；Python 编译、`pip check`、MySQL 连接、OpenAPI、前端类型检查、1752 modules 生产构建、生产依赖审计和真实浏览器桌面/390px 验收通过
- 设计：沿用轻盈实验室布告板基调，提供节点库、画布、缩放、节点配置和图检查；空图保持稳定缩放，已有图在布局稳定后自动适配，窄屏无页面横向溢出
- 安全：Workflow 所属 Project 和当前用户权限由 Service 校验，整图保存使用版本复查和事务，未记录密码、Token、数据库凭据或内部异常
- 依赖确认：用户明确批准 `@vue-flow/core@1.48.2`；pnpm 忽略间接 `vue-demi` 构建脚本，但直接 import、类型检查和生产构建均通过，未放宽脚本策略
- 已知限制：仓库仍无前端 Lint、组件测试和 E2E 脚本；P05 HERO 门属于既有参考图验收项，与 P09 功能无关

## 2026-09-02 12:46:24 +08:00

- 操作：完成 P10 ProjectContext 领域模型、持久化分层 API、节点权限与下游失效规则
- 目标：让 Project 和 P09 Workflow 共享可版本化上下文，支持创建、读取、修改、保存、节点受限读写和 stale 识别
- 原因：为 P11/P12 Agent 提供经过 Schema 校验且按项目所有权隔离的确定性上下文，本阶段不接 AI Provider 或执行 Workflow
- 结果：完成；复用 `projects.context_data`、`workflow_nodes.context_version/status`，新增 ContextBuilder、ContextManager、Repository、Service、Schema、Router 和 MySQL 验收脚本，未修改数据库结构或依赖
- 恢复方式：回退本阶段提交；`schema.sql` 和 18 张现有表无需回滚，真实 MySQL 验收临时用户、项目和 Workflow 已自动清理
- 验证：Python 编译、88 项后端测试、16 项 P10 专项测试、`pip check`、18 表/33 外键结构检查、40 条 OpenAPI 路径及真实 MySQL 版本 `1→2→3` 持久化验收通过
- 安全：所有 ProjectContext 和节点接口要求 JWT，Service 校验 Project 所有权；节点配置只能缩小字段白名单，外部 JSON 限制大小、深度并拒绝密码、Token、Secret、API Key 和原始模型输入键
- 已知限制：通用 `verify-backend.ps1` 固定调用系统 Python 的 `pytest`，与本仓库 `.venv + unittest` 约定不兼容；未新增 pytest、Ruff 或 Mypy 依赖，项目自身标准验证命令已通过

## 2026-09-02 13:07:47 +08:00

- 操作：完成 P11 统一 AI Provider、DeepSeek HTTP 适配、可靠性边界和受认证测试接口
- 目标：建立 `Router → AIService → AIClient → AIProvider → DeepSeek` 调用链，统一输入输出、错误分类、超时、取消、受限重试和脱敏日志
- 原因：为 P12 Agent 和 P13 Workflow Engine 提供不扩散供应商响应对象的模型调用边界，本阶段不实现 Agent、Workflow 执行或 AI 记录持久化
- 结果：完成；新增 `POST /api/v1/ai/test`，缺少密钥返回可操作的 503，真实 DeepSeek 接口和完整 API 路径均返回统一 JSON
- 恢复方式：回退本阶段提交并移除 `httpx` 直接依赖；本次未修改数据库结构、迁移、业务数据或前端代码
- 验证：Python 编译、102 项后端测试、`pip check`、41 条 OpenAPI 路径、Fake Provider、HTTP Mock 和真实 DeepSeek API 路径通过；真实接口返回 200、模型 `deepseek-v4-flash`、`finish_reason=stop`
- 安全：密钥只从已忽略的 `backend/.env` 读取，未输出或写入代码、日志、报告；日志不记录 Authorization、完整 Prompt、模型正文或个人数据
- 依赖确认：用户明确批准新增 `httpx>=0.28,<1.0` 和 DeepSeek 方案；实际安装 `httpx 0.28.1`，未安装 OpenAI、Anthropic 或其他供应商 SDK
- 已知限制：首次 8-token 真实请求因最终 `content` 为空被适配器正确拒绝，改为 64 tokens 后直连和 API 路径均通过；Ruff、Mypy、Pytest 未配置且未新增

## 2026-09-02 18:58:32 +08:00

- 操作：完成 P12 BaseAgent、ProjectAnalysisAgent、PromptAgent、ProjectReviewAgent、结构化结果 API 与 AIRequest/AIResult 持久化
- 目标：让核心 Agent 读取 P10 ProjectContext，经 P11 AIClient/AIProvider 单回合调用模型，并由 Service 保存和按当前用户查询结果
- 原因：为后续 P13 Workflow Engine 提供经过 Schema 校验、可审计且不直接操作数据库的 Agent 边界，本阶段不执行 Workflow 或模型生成命令
- 结果：完成；新增 3 个运行接口和 1 个结果查询接口，系统规则、Context、用户输入和模型输出分界，原始输入只保存 SHA-256 哈希和安全元数据
- 恢复方式：回退本阶段提交；本次未新增依赖、未修改 `schema.sql`、SQLAlchemy Models、18 张表或前端代码，MySQL 验收临时数据已自动清理
- 验证：18 项 P12 专项测试、121 项后端全量测试、Python 编译、`pip check`、45 条 OpenAPI 路径、真实 MySQL 持久化与跨用户隔离验收通过
- 真实 Provider：DeepSeek 原生 JSON 模式下，ProjectAnalysisAgent 和 PromptAgent 均返回 `finish_reason=stop` 并通过 Pydantic Schema；未记录 API Key、完整 Prompt 或模型正文
- 安全：Agent 单回合、总超时和 3000-token 默认上限明确；关闭 thinking；敏感输入和敏感模型输出均拒绝；不执行 shell、SQL、文件、工具或模型生成命令
- 范围：按优先级只实现 3 个可验证核心 Agent；未提前实现 LearningPlanAgent、LearningReportAgent、WorkflowAgent 或 P13 Workflow Engine

## 2026-09-02 19:42:06 +08:00

- 操作：完成 P13 Workflow 执行引擎、三节点 Agent 注册、运行状态持久化、Context 更新和 Run 结果查询
- 目标：串联 `WorkflowService → WorkflowEngine → Registry → Agent → AIClient → Provider → ContextManager → Repository → MySQL`，提供有界、可观测、可恢复的执行流程
- 原因：在 P09 编辑器、P10 Context、P11 Provider 和 P12 Agent 之上形成可运行的 AI Workflow 核心，不提前进入 P14 报告和统计
- 结果：完成；新增运行、运行列表和运行详情接口，支持需求分析、技术栈分析、架构设计的 DAG 顺序执行，以及技术栈变化后的下游 stale 和受影响节点重新生成
- 恢复方式：回退本阶段提交；本次未新增依赖、未修改 `schema.sql`、SQLAlchemy Models、18 张表或前端代码，MySQL 验收临时数据已自动清理
- 验证：12 项 P13 专项测试、133 项后端全量测试、Python 编译、`pip check`、48 条 OpenAPI 路径、MySQL 连接和 18 表/33 外键结构检查通过
- MySQL 验收：Fake Provider 完成 3 节点首次运行和 2 节点重新生成，保存 5 条结构化节点结果；活跃重复运行被拒绝，过期运行、请求和节点可恢复，临时用户残留为 0
- 可靠性：节点、回合、并发、总超时和 completion-token 预算均有上限；协程取消向上传播并持久化为 cancelled；只有 P11 AIClient 对明确短暂失败执行有界重试
- 安全：运行和结果查询按 Workflow 所属 Project 校验当前用户；模型结果经 Pydantic Schema 验证，日志只记录关联 ID、状态、耗时、Token 和失败类别，不记录密钥、完整 Prompt 或模型正文
- 已知限制：P13 固定顺序执行且不支持条件边或跨进程主动取消；跨进程中断通过下一次运行的超时恢复处理，真实 Provider 未在本阶段重复调用

## 2026-09-03 00:18:52 +08:00

- 操作：完成 P14 数据库聚合统计、ECharts 数据页和 AI 学习报告闭环
- 目标：提供今日、7/30 日趋势、项目完成、社区活跃和技术栈统计，并生成、保存和查询当前用户学习报告
- 原因：在 P06-P13 的真实社区、学习、Workflow 和 AI 数据基础上建立可说明来源的统计与报告，不为页面效果写死数据
- 结果：完成；新增 4 组统计 API、3 条学习报告 API、`/analytics` 页面、ECharts 图表、LearningReportAgent、增量迁移和真实 MySQL 验收脚本
- 恢复方式：代码可回退本阶段提交；数据库回退涉及 `project_views`、`projects.completed_at` 和新增索引，必须先评估已有 P14 数据，禁止直接删除表或列
- 验证：143 项后端测试、Python 编译、`pip check`、前端类型检查和生产构建通过；19 表/35 外键结构一致，真实 MySQL 聚合及报告刷新持久化通过
- 浏览器：桌面和 390×844 实测无整页横向溢出，7/30 日切换、空/错误/重试状态、真实 DeepSeek 报告和刷新读取通过；临时用户及数据残留为 0
- 安全：统计只返回匿名聚合；报告只读取当前用户聚合指标，模型输入输出经 Schema 校验，日志和数据库不保存密钥、认证头、完整敏感 Prompt 或学习记录正文
- 依赖与迁移确认：用户已批准 `echarts@6.1.0`、`project_views`、`projects.completed_at` 和时间索引；不回填历史完成时间，迁移预检部分应用状态并可安全重复执行
- 已知限制：统计页懒加载 chunk 约 572 KB，生产构建有 chunk-size 提示；功能与首屏主包不受影响，未在 P14 内引入额外拆包策略

## 2026-09-04 13:10:28 +08:00

- 操作：开展 P15 全系统联调，保存可继续执行的跨模块验收脚本、演示指南和进度检查点
- 目标：基于 P14 实际代码检查认证、社区、项目、学习、Workflow、Context、Provider、Agent、Engine、统计与报告，不新增业务功能
- 原因：单模块测试通过不足以证明跨模块状态、数据口径和前端会话一致，需要真实 HTTP/ASGI 与 MySQL 证据
- 结果：进行中；新增脚本复现工作台项目进度 100% 而报告输入为 0%，并定位报告沿用 5 秒请求超时、退出登录保留其他 Store 缓存两项基线问题
- 用户确认：已提出独立报告超时、退出/会话失效完整导航清理内存、统一任务完成率查询的最小方案，等待确认；未提前修改生产代码
- 验证：后端 143 项测试、Python 编译、pip check、前端类型检查和构建通过；真实数据库 19 表/35 外键与各模块验收通过；新增完整链路在进度一致性断言失败且临时数据已清理
- 运行：启动前后端，health、Swagger、登录页返回 200；未认证统计返回 401，允许和拒绝来源的 CORS 预检符合配置
- 未通过：生产依赖审计请求 npm advisories 重试后超时；未取得漏洞结论；浏览器完整交互与本阶段真实模型调用尚未验收
- 文档：修正 README 复制环境文件的覆盖风险、技术栈接口参数和迁移预检表述；记录 API 演示入口、Redis 未接入与现有简化边界
- 恢复方式：生产代码、依赖、结构与迁移未变动；继续时从 `开发相关文档/阶段报告/20260904-131028-P15-全系统联调检查点报告.md` 读取检查点，不重做 P00-P14，不创建 release 提交直到验收完成
- 安全：未输出密钥、密码、Token、完整认证头或敏感模型输入；本次测试只清理自己的临时账号及关联数据，不影响既有业务数据

## 2026-09-04 13:40:29 +08:00

- 操作：按用户确认完成 P15 三项最小修复、全链路联调、浏览器回归和答辩文档
- 目标：修复报告短超时、退出账号缓存残留、工作台/统计/报告项目进度不一致；不新增业务功能
- 用户确认：明确批准上述方案，不新增依赖、不修改数据库表结构
- 结果：报告独立 190 秒有界等待，退出/受保护会话失效完整导航；统计和报告共用数据库任务完成率查询，保留无任务回退和偶数取整，不回写字段或历史报告
- 验证：147 项后端测试、类型检查、2344 modules 构建、Python 编译、pip check 和 19 表/35 外键验证通过；Fake 与真实 DeepSeek 完整链路均通过，原失败断言保留
- 浏览器：真实报告约 8.6 秒后仍保存成功且刷新可读；重复提交拦截、跨账号查询失败不泄漏旧数据、错误密码和保护页 401 边界通过；新增独立回归脚本不依赖在线模型
- 画布与响应式：三节点拖拽、两条连线、保存刷新恢复通过；统计与 Workflow 桌面/390px 截图已核对，图表像素有效，无整页横向溢出
- 数据清理：所有本次临时账号及其关联数据已清理，最终 P15 临时账号数为 0；证据截图只包含本次临时样本
- 已知限制：外部依赖审计多种请求方式均超时，未取得漏洞结论；统计页 chunk 体积提示保留；Redis、管理员角色及 Context/Agent/Run 前端完整面板未实现，均在指南中明确
- 文档：更新启动说明、可演示接口、统计口径、简化边界、历史检查点和 `20260904-134029-p15-integration.md`，不把历史成功冒充本次验证
- 恢复方式：回退本次代码与文档提交即可；依赖、Models、schema.sql、迁移和持久化格式无变更，无需回滚数据库
- 提交：按约定使用 `release: complete graduation project integration`，推送后核对远端哈希

## 2026-09-04 18:08:26 +08:00

- 操作：按用户要求重新执行 P15 尚未完成的生产依赖安全审计
- 目标：`frontend` 的 `pnpm audit --prod`，不安装或升级依赖
- 结果：有界 30 秒请求仍超时；临时使用系统现有代理也超时；DNS 与 npm 首页 HEAD 正常，但漏洞接口空 POST 请求 15 秒无响应
- 结论：未取得漏洞数据，安全审计仍未完成；不将连通性检查或先前功能测试替代安全结论
- 影响与恢复：仅增加复查记录，源码、锁文件、数据库、系统代理和配置文件无变化；进程内临时代理变量已恢复
- 报告：`开发相关文档/阶段报告/20260904-180826-依赖安全审计复查报告.md`

## 2026-09-04 18:36:10 +08:00

- 操作：整理项目中不参与运行的规划、报告、日志、测试证据、设计资料和答辩材料
- 范围：不移动 `frontend/`、`backend/` 运行时代码及校验脚本；不修改依赖、数据库、环境变量或 API
- 分类：新增 `开发规划`、`开发日志`、`阶段报告`、`测试证据`、`答辩材料`、`界面设计` 分类，并保留 `AI提示词` 分类
- 重命名：将阶段报告、操作日志、答辩指南、P15 截图和 P09-P13 规划文件中的英文文件名改为中文或中英技术名组合
- 引用：同步更新根 README、后端说明、前端回归脚本、历史报告、答辩指南和阶段规划中的旧路径
- 入口约定：根 `README.md` 保留为 GitHub 项目入口，根 `PRODUCT.md` 保留为 Impeccable 产品设计清单；两者不参与运行
- 验证：旧路径扫描、Markdown 相对链接存在性检查和 Git 差异检查通过；本次无运行时代码变更，未重复执行前后端测试；远端提交核对待提交后完成
- 报告：`开发相关文档/阶段报告/20260904-183631-开发相关文档整理报告.md`

## 2026-09-04 18:40:11 +08:00

- 操作：完成开发相关文档整理提交并推送到 `origin/main`
- 提交：`b5df1243645b9f67f7099f6e7e49cfad43b08644`，提交信息为 `docs: organize development reports and records`
- 验证：本地与远端 `main` 提交哈希一致；工作区洁净；阶段报告 22 份、P15 测试证据 6 份；Markdown 相对链接复核通过

## 2026-09-04 19:06:21 +08:00

- 操作：按用户要求丰富登录页，增加主题切换、入场过渡、跟手形变滑块和服务端一次性凭证校验
- 范围：沿用 Vue/Element Plus/Axios/FastAPI；不新增依赖、数据库表或业务模块；复用现有插画
- 接口影响：新增挑战和验证接口，登录请求增加必填 `slider_token`；原 JWT、注册和已有数据不变，Swagger 文档与联调脚本同步调整
- 验证：后端 156 项测试、真实 MySQL/Fake Provider 联调、Python 编译、pip check、前端类型检查和生产构建通过；临时账号及关联数据清理完成
- 浏览器：真实拖动与登录、部分拖动回弹、错误密码后重试、会话隔离、401 清理、主题和键盘操作、到期提示、网络失败恢复、320/390/768/1440px 布局及减少动态效果通过
- 发现和修正：回归脚本首次未等待滑块回弹完成，第二次拖动未命中；增加稳定性等待后原断言通过。密码显隐按钮继承行高造成输入框不等高，也已修正
- 安全边界：基础滑块位置由客户端报告，不能替代成熟反机器人方案；只支持单 worker 内存挑战，重启后失效，未接入 Redis 或专业验证码服务
- 未执行：依赖和锁文件未变，未重跑原外部服务超时的安全审计；原统计 chunk 提示保留，未关闭检查
- 文档与证据：`开发相关文档/阶段报告/20260904-190621-登录页面与滑块验证报告.md`，截图统一归入 `开发相关文档/测试证据/登录页面优化/`
- 提交约定：`feat: enhance login experience with slider verification`，推送后核对远端哈希；前后端需同步部署或回退，无数据库迁移

## 2026-09-04 19:31:18 +08:00

- 操作：按用户要求，沿用原阶段格式生成 P16-P24 九份独立校园试点开发提示词
- 目标：`开发相关文档/AI提示词/`、提示词索引、文档分类入口和开发节点使用顺序；不开始业务开发
- 内容：设计确认、校园身份、班级任务、提交反馈、AI 页面、社区治理、质量成本、上线隐私和真实试点，均包含范围、确认点、验收与提交建议
- 技术边界：Vue/FastAPI/MySQL 单体，同一前端服务三类角色；不默认新增 Redis、消息队列、向量库、微服务或 Agent 框架
- 修正：索引不再把当前工程写成从零开始；原 P00-P15 提示词不改写，原时间和技术构想在执行顺序中标注历史背景
- 验证：九份文件编号、必要内容、编码与索引检查通过；补齐 P20 独立技术基线后复查；14 份文档的 32 个本地相对链接均存在，差异检查通过
- 未执行：代码测试、构建、数据库操作、依赖安装和学校试点均未执行，本次只生成 Markdown 文档
- 状态：P16-P24 仍为待执行；设计确认、技术验收、教师审阅和校方批准分别记录，不能互相替代
- 报告：`开发相关文档/阶段报告/20260904-193118-P16-P24-校园试点提示词生成报告.md`
- 提交约定：`docs: add campus pilot development prompts`，检查后推送并核对远端；恢复仅需回退文档差异，无运行或数据影响

## 2026-09-04 20:35:54 +08:00

- 操作：按用户要求后台启动 ScholarHub FastAPI 与 Vite，复用运行中的 MySQL。
- 验证：登录页面浏览器渲染正常；前端、健康接口和 Swagger 均 HTTP 200；应用数据库 SELECT 1 通过。
- 范围：没有业务代码、依赖、配置和数据库变更；未执行全量测试或 AI 调用。
- 恢复方式：核对归属后停止本次前后端进程，保留原 MySQL 服务。
- 报告：开发相关文档/阶段报告/20260904-203554-本地项目启动报告.md

## 2026-09-04 20:44:57 +08:00

- 操作：按用户选择的综合 6 个方案，通过已登录页面录入真实开源学习案例。
- 数据：项目 ID 34–39，每条包含来源、许可证、技术栈、中文导读、4 项实践任务及交付要求；账号为 jiaohaoyuan，未开始、进度 0、未发布。
- 验证：官方仓库及许可核对通过；浏览器列表与详情可见；只读数据库回读及现有 Schema 校验全部通过。
- 范围：未修改业务代码、接口、数据库结构或依赖；未运行第三方源码、模型或 Workflow。
- 恢复方式：按报告列出的项目 ID 核对后处理，涉及删除时另行确认。
- 报告：开发相关文档/阶段报告/20260904-204457-首批真实开源项目入库报告.md

## 2026-09-04 20:53:11 +08:00

- 操作：按用户要求录入 Python 原创入门练习 50 道，从颠倒字符串开始，编号 001–050。
- 数据：现有服务层创建项目 ID 40–89，归属 jiaohaoyuan；入门、未开始、进度 0、未发布。原 6 个开源案例保留。
- 内容：每题题意、函数约定、输入输出、提示及验收要求；五题补充空格说明。
- 验证：156 个示例参考计算与 50 个输入 Schema 校验通过；持久化回读共 56 条项目；浏览器列表与第一题详情可见。
- 文件：学习材料目录下的题目 JSON、目录和入库结果；本报告。无业务代码、依赖、API 或数据库结构变更。
- 恢复方式：按 ID 清单核对本批记录及关联数据，删除另行确认。
- 报告：开发相关文档/阶段报告/20260904-205311-Python入门50题录入报告.md

## 2026-09-04 21:42:48 +08:00

- 操作：按用户要求重启后端并检查 DeepSeek 接入。
- 原因：进程环境中的 Key 覆盖 .env，先前 401 来自被覆盖后的值。
- 处理：仅在本次启动进程环境移除 DEEPSEEK_API_KEY，让后端从 .env 读取；全局环境及配置文件不变。
- 验证：有效 Key 与 .env 一致；官方模型列表 HTTP 200，配置模型可见；后端健康检查 ok。未调用模型生成。
- 报告：开发相关文档/阶段报告/20260904-214248-后端重启与DeepSeek鉴权报告.md

## 2026-09-05｜工作流 AI 执行与教学节点

- 验证 `.env` DeepSeek Key 可用；以排除旧进程环境变量覆盖的启动会话重启后端。
- 补齐工作流运行入口、上下文准备、状态轮询、运行历史和结构化结果展示；新增解题提示、代码讲解、答案评审。
- 实测并修复只读教学节点空上下文补丁失败、代码配置变更后被误跳过的问题。
- 真实示例工作流 22，运行 28/29 完成三个节点；运行 30 仅重跑评审并指出错误答案。
- 后端 160 项测试、前端构建、pip check、差异空白检查通过。浏览器验证入口/续跑/历史与桌面手机结果布局，恢复默认视口。
- 验收报告：阶段报告/20260905-工作流AI入口与教学节点验收.md。

## 2026-09-05｜项目库文字排版适配（+08:00）

- 修复长说明撑满卡片、状态标签竖排；标题加大加粗并限制两行，摘要三行省略，统一标签、更新时间和操作入口。
- 样式限定项目库，保留完整项目原文与翻面交互；手机翻面按钮调整两列。
- 前端构建和差异检查通过；桌面/手机实测展示与翻面，恢复默认视口。
- 报告：阶段报告/20260905-项目库文字排版适配.md。恢复时只撤销本次项目卡片排版段。


## 2026-09-05 00:30 +08:00｜P16 校园试点设计草案

- 只读核对项目规则、实际路由/Models/schema.sql/迁移/认证/AI/前端与最新报告；区分当前工作区实现、仅 API、缺失及待确认项。
- 新增唯一设计正文：开发规划/P16_校园试点最小方案与数据设计草案.md，覆盖权限、核心 ER、提交快照、状态与 API、迁移恢复、隐私及 P17–P24 依赖。
- 设计草案已完成；用户设计确认、教师确认、校方批准均未取得，P17 未开始。新 API/数据库/JWT/部署建议需逐项确认。
- 仅做文档检查，不运行测试/构建、数据库/模型调用或业务验收，不修改运行代码、真实账号或服务。
- 报告：阶段报告/20260905-P16-校园试点设计草案报告.md。仅提交本次三份文档及日志追加，既有工作区改动保留；按约定正常推送并在最终汇报核对远端。
- 恢复：后续文档修订标记废止即可，不涉及数据库或运行时恢复。

## 2026-09-05 00:42 +08:00｜撤回 P17 实施

- 按用户“撤回这个操作”恢复刚才 P17 涉及的六份既有后端文件，删除三份新增 campus 模块；保留此前教学节点、工作流、排版和 P16 文档。
- 差异核对：五份文件与 HEAD 一致，deps.py 仅保留此前教学节点注册；后端源码无 P17 身份版本和 campus 引用残留。
- P17 尚未执行数据库迁移、服务重启或真实账号初始化；本次未提交或推送 GitHub。仅检查撤回差异，未运行代码测试。
- 报告：阶段报告/20260905-P17-撤回实施记录.md。停止 P17 开发，后续重启需用户明确指示。

## 2026-09-05 13:16 +08:00｜P17 校园身份与账号管理

- 用户再次确认 P16 的 D1–D4 推荐方案并要求先实施、暂不提交 GitHub；保留旧个人账号与私人数据，不创建真实校园成员。
- 新增固定学生/教师/管理员角色、定向一次性邀请、邮箱邀请原子注册、成员兑换、账号启停/资格/角色调整、改密、一次性重置、全部会话撤销、审计和最后管理员保护。
- `users` 增加 `auth_version`，JWT 增加 `ver` 并在每次请求查库；上线后旧 JWT 需重新登录。新增 campus_memberships、campus_invitations、password_resets、account_audits 四表，无新增依赖。
- 迁移前使用 mysqldump 完成 152692 字节备份，保存于工作区外 ScholarHub-backups；应用 P17 增量迁移后，实际 MySQL 的 23 张表、44 个约束与索引和 Models 一致。
- 隔离集成验收覆盖首管理员、定向邀请、两路并发兑换只生效一次、改密、重置重放拒绝、最后管理员保护、邀请注册 HTTP 链路和审计；临时账号与五类身份数据均清理为 0。
- 后端 167 项测试、pip check、前端生产构建、git diff --check 通过；Ruff 未安装，未新增依赖。浏览器验证登录重定向、公开重置页桌面和 390px 窄屏，恢复默认视口。
- 后端与前端已重启并监听 8000/5173，health=ok，匿名校园身份接口返回 401。未初始化真实管理员；教师确认和校方批准仍待取得。
- 报告：阶段报告/20260905-P17-校园身份与账号管理验收.md。恢复代码需回退本阶段文件；已迁移数据库优先前向修复，备份恢复前必须冻结写入并确认数据损失。本轮不提交、不推送 GitHub。

## 2026-09-06 16:33:00 +08:00｜审核报告最小修改优化

- 按用户要求落实局部修复，保留现有接口与架构：89 个纯同步路由改为 def；补齐工作台任务和记录校验、认证限流、工作流状态校验；校园命名锁固定连接至释放完成。
- 前端修正未应用配置的保存提示、教学字段残留、重命名旧状态回写及工具栏样式冲突；工作台记录表单引导课程和任务记录使用已有学习系统入口。
- 删除确认不可达的 HomeView 与前端 health store/API/type 四文件，清理未使用导入、不可达表达式和失效状态变量；保留后端健康接口。
- 验证：修改前 167 项、修改后 183 项后端测试通过，新增 16 项定向回归；pip check、类型检查、生产构建、OpenAPI 完整对比和差异检查通过。真实 MySQL 双连接验证提交、回滚后的命名锁释放，未修改业务数据。
- 浏览器使用虚构 API 响应验证配置、保存、重命名与记录关联，以及 390–1440px 五种宽度的工具栏；未调用真实 AI。AnalyticsView 571.90 kB 提示仍保留。
- 新增回归脚本和阶段报告 `阶段报告/20260906-1633-审核报告最小修改优化.md`。未新增依赖或迁移，未提交、推送；保留此前未提交内容。
- 恢复方式：仅回退本轮差异；删除文件可从 Git 历史单独恢复。大型重构、混合 AI 异步链路和新质量门禁留待单独处理。

## 2026-09-07 23:01:00 +08:00｜P18 班级与教学任务

- 用户确认 D5 推荐方案并授权实施；新增独立教学班、班级成员和教学任务，不改写个人 Course、DailyTask、Project，不进入 P19 提交反馈。
- 后端保持 Router → Service → Repository → Model；教师按班级授权，学生只见本人班级非草稿任务，管理员无任务正文权限；写操作使用 revision。
- 任务状态 `draft → published → closed → archived`；发布后冻结题意和验收字段，只允许延长截止时间。项目模板保存教师本人项目的白名单快照，不包含 Context 或仓库地址。
- 新增三个前端入口，提供班级创建/编辑/归档、成员加入/移除/退出、任务草稿/发布/截止时间/关闭/归档及学生只读展示。
- 迁移前备份 160307 字节到工作区外。首次迁移因一个自动外键名超过 MySQL 64 字符限制而停在两张空表；缩短约束名后只补建缺失表，最终实际数据库 26 张表、51 个外键并与 Models 一致。
- 验证：192 项后端测试、实际 MySQL/API 双教师双班流程、Python 编译、pip check、前端类型检查、生产构建和差异检查通过；隔离测试数据清理为 0。
- 收口检查发现三条 P18 前端路由重复注册，删除重复块后确认每个路由名仅出现一次，并重新通过类型检查与生产构建。
- 浏览器确认未登录访问教学班会回到带 redirect 的登录页；因不保留虚构校园账号，未执行登录后的角色页面视觉验收。真实教师确认和校方批准仍未取得。
- 服务已在 127.0.0.1:8000 与 127.0.0.1:5173 启动；本轮不提交、不推送。报告：`阶段报告/20260907-P18-班级与教学任务验收.md`。

## 2026-09-07 23:21:24 +08:00｜初始化首个校园管理员

- 用户明确授权将现有账号 jiaohaoyuan（user_id=12）设置为校园管理员。
- 通过受控命令 python -m app.cli.init_campus_admin 完成初始化；生成 campus_membership_id=11，角色 administrator，状态 active，revision=1。
- 初始化将 auth_version 更新为 1，旧登录会话失效；用户需重新登录后访问校园账号管理页。
- 已查询实际 MySQL 核对账号、校园身份与 initialize_first_admin/success 审计记录；未创建教师或学生账号，未提交或推送 GitHub。

## 2026-09-07 23:40:17 +08:00｜整理并提交当前交付

- 用户明确授权提交并推送当前工作区，同时更新 README 等说明；提交范围包含此前已验收但未提交的工作流教学入口、P17、规范审查最小修复、P18、学习材料、知识网络和阶段报告。
- 根 README、后端 README、开发文档入口和知识网络更新到 P18；校正旧的三节点、19 张表、无校园角色和无运行面板等过期表述，明确 P19-P24、教师确认和真实试点仍未完成。
- 检查发现知识网络中的嵌套 Obsidian 仓库是顶层 22 篇笔记的逐文件重复副本，保留本机内容并通过 `.gitignore` 排除；移除不存在的 Canvas 链接，改为 Obsidian 内置关系图谱入口。
- 提交前检查：后端 192 项测试通过；实际 MySQL 26 张表、51 个外键与 Models 一致；`pip check` 通过；前端 2369 模块生产构建通过；`git diff --check` 通过。
- 浏览器回归首次因项目未安装 Playwright 无法启动；未新增依赖，随后使用 Codex 外部 Playwright 和本机 Edge 复跑，通过配置状态、保存阻断、教学字段、重命名、项目关联和 5 种宽度检查。
- 密钥模式检查只命中用于拒绝敏感内容的测试样例和 CSS 类名；`.env`、数据库备份、构建产物、Obsidian 状态和邀请凭证均未纳入提交。

## 2026-09-11 10:13:00 +08:00｜P20 AI 功能页面闭环

- 复核现有 ProjectContext、Agent 和 Workflow API，确认可用现有接口完成 P20；未新增依赖、公共 API、数据库结构、迁移、Agent 类型或执行引擎。
- 新增项目级 AI 工作台：Context 创建/查看/编辑、expected_version 冲突恢复、显式同步项目字段、字段来源与受影响节点；需求分析和开发 Prompt 均可从页面主动运行、按请求编号查询并复制结构化结果。
- Workflow 编辑器改为只读检查 Context，缺失、字段漂移或读取失败时阻止运行并指向 AI 工作台；移除原先运行前静默创建/同步 Context 的行为，显示受影响节点并保留同步运行、历史记录和失败续跑边界。
- 浏览器使用虚构登录态、Project、Context 和 fake Provider 验证 Context 创建、409 草稿保留、显式同步、两类 Agent、结果查询/复制、三节点 Workflow、长文本、1440px 与 390px；三张脱敏截图保存到 `测试证据/`，没有访问真实数据库或模型。
- 验证：后端 203 项测试通过；前端类型检查、2382 模块生产构建、P20 浏览器脚本和既有前端回归脚本通过。真实 Provider、真实模型费用和真实 MySQL 浏览器链路未执行，未冒充通过。
- 报告：`阶段报告/20260911-P20-AI功能页面闭环验收.md`。P21-P24、教师确认、校方批准和真实课堂试点仍未开始。
